function calculateAll(sum, sub, mul, div) {
    return {
        sum: sum,
        sub: sub,
        mul: mul,
        div: div,
    };
};


const result = calculateAll(10, 5, 2, 3);
console.log(result);

function calculateAllExpression = function(sum, sub, mul, div) {
    return {
        sum: sum,
        sub: sub,
        mul: mul,
        div: div,
    };
};

const resultExpression = calculateAllExpression(10, 5, 2, 3);
console.log(calculateAllExpression);

const calculateAllArray2 = sum(sum, sub, mul, div) => {
    return {
        sum: sum,
        sub: sub,
        mul: mul,
        div: div,
    };
};

const resultArray2 = calculateAllArray2(10, 5, 2, 3);
console.log(calculateAllArray2);